import ComingSoon from '../components/ComingSoon';
import Layout from '../components/Layout';

const Boxing = () => {
  return (
    <Layout title={'Boxing'} breadcrums={'Boxing'}>
      <ComingSoon />
    </Layout>
  );
};

export default Boxing;
